

/**
 * ECG and EGM listeners are notified with real time tics from <code>RealtimeThread</code>.
 * Listeners are registered to <code>RealtimeThread</code>. 
 * Listeners are notified with a stream that could be changed in run time through
 * the uMock interface.
 */

package org.jprotocol.framework.realtime;
